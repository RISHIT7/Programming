let age = 17;

if (age >= 18) {
  console.log("You can drive");
} else {
  console.log("You cannot drive");
}

var x = 11;
var y = 9;
var b = x <= y;
console.log(b); // returns boolean values

var z = "11";
c = x === z;
d = x == z;
console.log(c); // also the type
console.log(d); // only the value
// (and) == (&&) from logic gates
// (not) == (!) from logic gates
// (or) == (||) from logic gates